const SectionContainer = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="max-w-3xl mx-auto px-4 sm:px-6 xl:px-0">{children}</div>
  );
};

export default SectionContainer;
